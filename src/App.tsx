import SignIn from "./LoginPage";

const App: React.FC = () => {
  // const submitLoginHandler = (email: string, password: string) => {
  //   if(email === "")
  //   console.log(email, password);
  // };
  // return <SignIn onSubmit={submitLoginHandler} />;
  return <SignIn />;
};

export default App;
